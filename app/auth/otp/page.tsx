export const metadata = { title: "Phone OTP" };

export default function OtpPage() {
  return (
    <div className="min-h-screen flex items-center justify-center p-8">
      <div className="max-w-sm">
        <h1 className="font-serif text-4xl">Phone OTP</h1>
        <p className="mt-4 text-ink-soft">
          Architecture placeholder. Supabase phone OTP will be enabled in Stage 2 once the SMS provider is approved.
        </p>
      </div>
    </div>
  );
}
