-- Hosted seed is applied after auth users exist.
-- Local preview seeds via lib/db/seed.ts (file store).
-- Development accounts (NOT production people):
--   admin@varizel.dev / Admin123!Dev
--   ananya@varizel.dev / Customer123!
--   kabir@varizel.dev / Customer123!
-- Coupons: WELCOME10, FLAT100. OLD50 is expired.

select 'Apply application seed from the Next.js local store or a future supabase seed job.';
